apiVersion: v1
kind: Service
metadata:
    name: "kubecluster-ab68"
    labels:
        app: "kubecluster-ab68"
spec:
    type: ClusterIP
    ports:
    - port: 80
      targetPort: 80
      protocol: TCP
      name: http
    selector:
        app: "kubecluster-ab68"